# --- обязательный минимум (данные + табличные модели + графики) ---
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
scikit-learn>=1.3
lightgbm>=4.0
xgboost>=2.0
matplotlib>=3.7
joblib>=1.3

# --- эксперименты в ноутбуках (по желанию; можно закомментить) ---
jupyter>=1.0
ipykernel>=6.0

# --- бэктест/векторные операции (по желанию; тяжелее по зависимостям) ---
# vectorbt>=0.26

# --- конфиги (по желанию) ---
# python-dotenv>=1.0
# pydantic>=2.0

# --- нейросети: ставьте отдельно под CPU/GPU с сайта PyTorch, не из этого файла ---
# pip install torch --index-url https://download.pytorch.org/whl/cpu
